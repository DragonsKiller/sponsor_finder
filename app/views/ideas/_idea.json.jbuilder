json.extract! idea, :id, :name, :description, :problem, :industry, :team, :geographical_focus, :requirements, :next_steps, :publication_end_date, :created_at, :updated_at
json.url idea_url(idea, format: :json)
